{
  "buildCommand": "python3 -m pip install uv && python3 -m uv pip install . --target /build && uv sync",
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ],
  "outputDirectory": "."
}
